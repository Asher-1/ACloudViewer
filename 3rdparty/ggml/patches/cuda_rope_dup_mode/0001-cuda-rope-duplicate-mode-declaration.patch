diff --git a/src/ggml-cuda/rope.cu b/src/ggml-cuda/rope.cu
--- a/src/ggml-cuda/rope.cu
+++ b/src/ggml-cuda/rope.cu
@@ -667,7 +667,8 @@
 
     //const int n_past     = ((int32_t *) dst->op_params)[0];
     const int n_dims     = ((int32_t *) dst->op_params)[1];
-    const int mode       = ((int32_t *) dst->op_params)[2];
+    // `mode` is already declared at the top of this function; nvcc <= 11.8
+    // rejects the duplicate declaration ("mode has already been declared").
     //const int n_ctx      = ((int32_t *) dst->op_params)[3];
     const int n_ctx_orig = ((int32_t *) dst->op_params)[4];
     const int n_offs     = ((int32_t *) dst->op_params)[15];
