diff --git a/src/ggml-vulkan/ggml-vulkan.cpp b/src/ggml-vulkan/ggml-vulkan.cpp
index c3fd436..7ff0c0a 100644
--- a/src/ggml-vulkan/ggml-vulkan.cpp
+++ b/src/ggml-vulkan/ggml-vulkan.cpp
@@ -19060,4 +19060,6 @@ static void ggml_vk_check_results_1(ggml_backend_vk_context * ctx, ggml_cgraph *
 }
 #endif
 
+#include "ggml-vulkan-aliked.inc.cpp"
+
 GGML_BACKEND_DL_IMPL(ggml_backend_vk_reg)
diff --git a/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp b/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
index 58d347b..b865bb1 100644
--- a/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
+++ b/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
@@ -1138,6 +1138,18 @@ void process_shaders() {
 
     string_to_spv("topk_moe_f32", "topk_moe.comp", {});
 
+    // ALIKED custom compute (LightGlue-GGML Phase 5d)
+    string_to_spv("aliked_deform_conv_f32", "aliked_deform_conv.comp", {});
+    string_to_spv("aliked_layout_convert_f32", "aliked_layout_convert.comp", {});
+    string_to_spv("aliked_dense_copy_f32", "aliked_dense_copy.comp", {});
+    string_to_spv("aliked_clamp_f32", "aliked_clamp.comp", {});
+    string_to_spv("aliked_max_pool_f32", "aliked_max_pool.comp", {});
+    string_to_spv("aliked_dkd_elem_f32", "aliked_dkd_elem.comp", {});
+    string_to_spv("aliked_block_topk_f32", "aliked_block_topk.comp", {});
+    string_to_spv("aliked_dkd_refine_f32", "aliked_dkd_refine.comp", {});
+    string_to_spv("aliked_sddh_f32", "aliked_sddh.comp", {});
+    string_to_spv("aliked_upsample_bilinear_f32", "aliked_upsample_bilinear.comp", {});
+
     for (auto &c : compiles) {
         c.wait();
     }
