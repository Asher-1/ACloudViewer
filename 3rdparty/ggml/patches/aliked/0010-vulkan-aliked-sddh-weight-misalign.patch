diff --git a/src/ggml-vulkan/ggml-vulkan-aliked.inc.cpp b/src/ggml-vulkan/ggml-vulkan-aliked.inc.cpp
--- a/src/ggml-vulkan/ggml-vulkan-aliked.inc.cpp
+++ b/src/ggml-vulkan/ggml-vulkan-aliked.inc.cpp
@@ -76,6 +76,7 @@ struct vk_aliked_push_sddh {
     uint32_t feature_off, kpt_off, workspace_off, desc_off;
     uint32_t k_offset, batch_count;
     uint32_t src_nb0, src_nb1, src_nb2;
+    uint32_t off0_w, off0_b, off2_w, off2_b, sf_w, agg_w;
 };
 
 struct vk_aliked_push_upsample {
@@ -558,6 +559,12 @@ bool ggml_vulkan_aliked_run_sddh(
     const uint32_t kpt_off = vk_aliked_misalign_floats(ctx, keypoints_norm);
     const uint32_t workspace_off = vk_aliked_misalign_floats(ctx, workspace);
     const uint32_t desc_off = vk_aliked_misalign_floats(ctx, descriptors);
+    const uint32_t off0_w = vk_aliked_misalign_floats(ctx, offset_0_w);
+    const uint32_t off0_b = vk_aliked_misalign_floats(ctx, offset_0_b);
+    const uint32_t off2_w = vk_aliked_misalign_floats(ctx, offset_2_w);
+    const uint32_t off2_b = vk_aliked_misalign_floats(ctx, offset_2_b);
+    const uint32_t sf_w = vk_aliked_misalign_floats(ctx, sf_conv_w);
+    const uint32_t agg_w = vk_aliked_misalign_floats(ctx, agg_weights);
 
     const int32_t num_batches = (count + kSddhBatchSize - 1) / kSddhBatchSize;
 
@@ -569,7 +576,8 @@ bool ggml_vulkan_aliked_run_sddh(
                                feature_off, kpt_off, workspace_off, desc_off,
                                static_cast<uint32_t>(start), static_cast<uint32_t>(batch),
                                static_cast<uint32_t>(feature_map->nb[0]),
                                static_cast<uint32_t>(feature_map->nb[1]),
-                               static_cast<uint32_t>(feature_map->nb[2])};
+                               static_cast<uint32_t>(feature_map->nb[2]),
+                               off0_w, off0_b, off2_w, off2_b, sf_w, agg_w};
         vk_aliked_dispatch1(
             ctx, pipes.sddh,
diff --git a/src/ggml-vulkan/vulkan-shaders/aliked_sddh.comp b/src/ggml-vulkan/vulkan-shaders/aliked_sddh.comp
--- a/src/ggml-vulkan/vulkan-shaders/aliked_sddh.comp
+++ b/src/ggml-vulkan/vulkan-shaders/aliked_sddh.comp
@@ -19,6 +19,13 @@ layout(push_constant) uniform parameter {
     uint src_nb0;
     uint src_nb1;
     uint src_nb2;
+    uint off0_w;
+    uint off0_b;
+    uint off2_w;
+    uint off2_b;
+    uint sf_w;
+    uint agg_w;
 } p;
 
 layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
@@ -125,7 +132,7 @@ void main() {
     uint desc_base = transformed_base + p.dim;
 
     for (uint oc = 0u; oc < 32u; ++oc) {
-        float sum = offset_0_b[oc];
+        float sum = offset_0_b[p.off0_b + oc];
         for (uint ic = 0u; ic < p.dim; ++ic) {
             for (uint ky = 0u; ky < p.kernel_size; ++ky) {
                 for (uint kx = 0u; kx < p.kernel_size; ++kx) {
@@ -132,7 +139,7 @@ void main() {
                         ic * p.kernel_size * p.kernel_size + ky * p.kernel_size + kx;
                     sum += workspace[base + ic * p.kernel_size * p.kernel_size + ky * p.kernel_size + kx] *
-                        offset_0_w[widx];
+                        offset_0_w[p.off0_w + widx];
                 }
             }
         }
@@ -140,9 +147,9 @@ void main() {
     }
 
     for (uint oc = 0u; oc < 32u; ++oc) {
-        float sum = offset_2_b[oc];
+        float sum = offset_2_b[p.off2_b + oc];
         for (uint ic = 0u; ic < 32u; ++ic) {
-            sum += workspace[off_raw_base + ic] * offset_2_w[oc * 32u + ic];
+            sum += workspace[off_raw_base + ic] * offset_2_w[p.off2_w + oc * 32u + ic];
         }
         workspace[off_final_base + oc] = sum;
     }
@@ -174,7 +181,7 @@ void main() {
         for (uint c = 0u; c < p.dim; ++c) {
             float value = 0.0;
             for (uint ic = 0u; ic < p.dim; ++ic) {
-                value += workspace[sampled_base + ic] * sf_conv_w[c * p.dim + ic];
+                value += workspace[sampled_base + ic] * sf_conv_w[p.sf_w + c * p.dim + ic];
             }
             workspace[transformed_base + c] = selu(value);
         }
@@ -182,7 +189,7 @@ void main() {
         for (uint c = 0u; c < p.dim; ++c) {
             for (uint ic = 0u; ic < p.dim; ++ic) {
                 workspace[desc_base + c] += workspace[transformed_base + ic] *
-                    agg_weights[pos * p.dim * p.dim + ic * p.dim + c];
+                    agg_weights[p.agg_w + pos * p.dim * p.dim + ic * p.dim + c];
             }
         }
     }
